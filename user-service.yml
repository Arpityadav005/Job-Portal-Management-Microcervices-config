server:
  port: 6001

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobuser_db
    username: arpit
    password: arp01

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
