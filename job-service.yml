server:
  port: 6002   

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/job_db
    username: arpit
    password: arp01

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
