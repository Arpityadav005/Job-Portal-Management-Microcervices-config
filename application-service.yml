server:
  port: 6003

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/application_db
    username: arpit
    password: arp01

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
